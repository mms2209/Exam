# Supabase Configuration
# Get these values from your Supabase project dashboard at https://supabase.com/dashboard

# Frontend Environment Variables (Required)
VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Important: The following environment variables must be configured in your Supabase project dashboard
# DO NOT add them to this .env file as they are server-side secrets

# Supabase Edge Function Secrets (Configure in Supabase Dashboard)
# To configure these secrets:
# 1. Go to your Supabase Dashboard
# 2. Navigate to Project Settings → Edge Functions → Secrets
# 3. Add the following secrets:

# GEMINI_API_KEY (Required for AI Chat Feature)
# Get your Gemini API key from: https://makersuite.google.com/app/apikey
# This is used by the exam-chat-ai Edge Function to power the AI tutor feature

# Note: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, and SUPABASE_DB_URL
# are automatically available in Edge Functions and do not need to be configured manually.
